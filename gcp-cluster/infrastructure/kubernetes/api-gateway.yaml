apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: payasam
  labels:
    app: api-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
        - name: api-gateway
          image: payasam/api-gateway:phase4
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          env:
            - name: ORDER_SERVICE_URL
              value: "http://order-service:8000"
          envFrom:
            - configMapRef:
                name: telemetry-config
                optional: true
            - secretRef:
                name: openobserve-credentials
                optional: true
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "150Mi"
              cpu: "250m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 3
            periodSeconds: 10
            timeoutSeconds: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 3
            periodSeconds: 5
            timeoutSeconds: 4
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: payasam
spec:
  selector:
    app: api-gateway
  ports:
    - port: 8000
      targetPort: 8000
