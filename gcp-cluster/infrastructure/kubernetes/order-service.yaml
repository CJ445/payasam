apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: payasam
  labels:
    app: order-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
        - name: order-service
          image: payasam/order-service:phase4
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          env:
            - name: INVENTORY_SERVICE_URL
              value: "http://inventory-service:8000"
            - name: PAYMENT_SERVICE_URL
              value: "http://payment-service:8000"
            - name: DATABASE_URL
              value: "http://database:8000"
          envFrom:
            - configMapRef:
                name: telemetry-config
                optional: true
            - secretRef:
                name: openobserve-credentials
                optional: true
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "150Mi"
              cpu: "250m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 3
            periodSeconds: 10
            timeoutSeconds: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 3
            periodSeconds: 5
            timeoutSeconds: 4
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: payasam
spec:
  selector:
    app: order-service
  ports:
    - port: 8000
      targetPort: 8000
